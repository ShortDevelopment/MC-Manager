<UserControl
    x:Class="MC_Manager.Controls.LinkButtonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MC_Manager.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="250" Height="130">

    <Button BorderThickness="1" BorderBrush="Black" CornerRadius="5" Padding="10" Style="{StaticResource ButtonRevealStyle}" Background="Transparent"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
            Click="Button_Click">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{x:Bind Title}" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,10" HorizontalAlignment="Center" />
            <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="{x:Bind Description}" />
        </Grid>
    </Button>
</UserControl>
